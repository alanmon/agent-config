import { createRoot } from 'react-dom/client';
export function createReactRoot(container) {
    const root = createRoot(container);
    return {
        render: (element) => root.render(element),
        unmount: () => root.unmount(),
    };
}
//# sourceMappingURL=latest.js.map