import { jsx as _jsx } from "@emotion/react/jsx-runtime";
export default function ViewerNav(props) {
    const { activeIndex = 0 } = props;
    function handleChangeImg(newIndex) {
        if (activeIndex === newIndex) {
            return;
        }
        props.onChangeImg(newIndex);
    }
    const marginLeft = `calc(50% - ${activeIndex + 1} * 31px)`;
    const listStyle = {
        marginLeft,
    };
    return (_jsx("div", { className: `${props.prefixCls}-navbar`, children: _jsx("ul", { className: `${props.prefixCls}-list ${props.prefixCls}-list-transition`, style: listStyle, children: props.images.map((item, index) => (_jsx("li", { className: index === activeIndex ? 'active' : '', onClick: () => {
                    handleChangeImg(index);
                }, children: _jsx("img", { src: item.src, alt: item.alt }) }, index))) }) }));
}
//# sourceMappingURL=ViewerNav.js.map