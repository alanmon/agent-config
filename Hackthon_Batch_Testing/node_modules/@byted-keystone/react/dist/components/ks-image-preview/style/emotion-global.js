import { jsx as _jsx } from "@emotion/react/jsx-runtime";
import { Global, css } from '@emotion/react';
import { ks } from '@fe-infra/keystone-design-tokens';
export function EmotionGlobalStyles({ prefixCls }) {
    const zIndex = 1000;
    const viewerListItemWidth = 30;
    const viewerListItemHeight = 50;
    return (_jsx(Global, { styles: css `
        .${prefixCls} {
          opacity: 0;
          position: relative;
          z-index: ${zIndex};
        }

        .${prefixCls}-inline {
          position: relative;
          width: 100%;
          height: 100%;
          min-height: 400px;
        }

        .${prefixCls} ul {
          margin: 0;
          padding: 0;
        }

        .${prefixCls} li {
          list-style: none;
        }

        .${prefixCls}-backdrop {
          position: fixed;
          top: 0;
          right: 0;
          left: 0;
          bottom: 0;
          background-color: ${ks.color.neutral.overlay};
          height: 100%;
          filter: alpha(opacity=50);
          z-index: ${zIndex};
        }

        .${prefixCls}-close {
          position: fixed;
          top: 32px;
          right: 32px;
          z-index: ${zIndex + 10};

          &::part(base) {
            color: ${ks.color.neutral.onFill};
            background-color: ${ks.color.neutral.overlay};
            border-color: ${ks.color.neutral.overlay};
            background-clip: padding-box;

            &:hover {
              background-color: ${ks.color.neutral.fillMedHighHover};
              border-color: ${ks.color.neutral.fillMedHighHover};
            }
          }
        }

        .${prefixCls}-close > i {
          position: relative;
          top: 4px;
          left: 18px;
        }

        .${prefixCls}-canvas {
          position: fixed;
          top: 0;
          right: 0;
          left: 0;
          bottom: 0;
          overflow: hidden;
          z-index: ${zIndex + 5};
        }

        .${prefixCls}-canvas > img {
          display: block;
          width: auto;
          height: auto;
          user-select: none;
        }

        .${prefixCls}-canvas > img.drag {
          cursor: move;
        }

        .${prefixCls}-footer {
          position: fixed;
          right: 0;
          bottom: 0;
          left: 0;
          overflow: hidden;
          text-align: center;
          z-index: ${zIndex + 5};
        }

        .${prefixCls}-inline
          > .${prefixCls}-backdrop,
          .${prefixCls}-inline
          > .${prefixCls}-close,
          .${prefixCls}-inline
          > .${prefixCls}-canvas,
          .${prefixCls}-inline
          > .${prefixCls}-footer {
          position: absolute;
        }

        .${prefixCls}-attribute {
          margin: 0 20px;
          margin-bottom: 6px;
          opacity: 0.8;
          color: #ccc;
          font-size: 15px;
        }

        .${prefixCls}-showTotal {
          float: right;
        }

        .${prefixCls}-toolbar {
          &-container {
            position: fixed;
            bottom: 40px;

            display: flex;
            flex-direction: column;
            width: 100%;
            justify-content: center;
            align-items: center;
          }

          display: flex;
          width: fit-content;
          gap: ${ks.spacing[2]};
          padding: ${ks.spacing[1]} ${ks.spacing[2]};
          background-color: ${ks.color.neutral.surface};
          border-radius: ${ks.border.radius.lg};
        }

        .${prefixCls}-navbar {
          overflow: hidden;
          background-color: rgba(0, 0, 0, 0.5);
        }

        .${prefixCls}-list {
          height: ${viewerListItemHeight}px;
          padding: 1px;
          text-align: left;
        }

        .${prefixCls}-list > li {
          display: inline-block;
          width: ${viewerListItemWidth}px;
          height: ${viewerListItemHeight}px;
          cursor: pointer;
          overflow: hidden;
          margin-right: 1px;
        }

        .${prefixCls}-list > li.active > img {
          opacity: 1;
        }

        .${prefixCls}-transition {
          transition: opacity 0.3s ease-out;
        }

        .${prefixCls}-list-transition {
          transition: margin 0.3s ease-out;
        }

        .${prefixCls}-icon {
          /* 已移除 font-family: 'icomoon' */
          display: inline-block;
          font-style: normal;
          vertical-align: baseline;
          text-align: center;
          text-transform: none;
          text-rendering: auto;
          line-height: 1;
          text-rendering: optimizeLegibility;
          -webkit-font-smoothing: antialiased;
          -moz-osx-font-smoothing: grayscale;
          color: white;
          font-size: 13px;
        }
      ` }));
}
//# sourceMappingURL=emotion-global.js.map