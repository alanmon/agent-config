import { ActionType } from './Icon';
export interface ViewerImageSize {
    width: number;
    height: number;
}
export interface ImageDecorator {
    src: string;
    alt?: string;
    downloadUrl?: string;
    defaultSize?: ViewerImageSize;
}
export interface ToolbarConfig {
    key: string;
    actionType?: ActionType;
    render?: React.ReactNode;
    onClick?: (activeImage: ImageDecorator) => void;
}
export interface ViewerDefaultImg {
    src: string;
    width?: number;
    height?: number;
}
interface ViewerProps {
    /**
     * @locale {en} Whether the viewer is open
     * @locale {zh} 查看器是否可见
     */
    open?: boolean;
    /**
     * @locale {en} Callback when close button is clicked
     * @locale {zh} 点击关闭按钮的回调
     */
    onClose?: (e: React.MouseEvent | KeyboardEvent) => void;
    /**
     * @locale {en} Images to be browsed
     * @locale {zh} 需要进行浏览的图片地址集合
     */
    images?: ImageDecorator[];
    /**
     * @locale {en} Current image index
     * @locale {zh} 当前图像index
     */
    activeIndex?: number;
    /**
     * @locale {en} Default active image index
     * @locale {zh} 默认激活的图片index
     */
    defaultActiveIndex?: number;
    /**
     * @locale {en} Custom viewer component z-index
     * @locale {zh} 自定义viewer组件的z-index
     */
    zIndex?: number;
    /**
     * @locale {en} Viewer render parent node, enable inline mode when set
     * @locale {zh} viewer渲染的父节点，设置后开启inline mode
     */
    container?: HTMLElement;
    /**
     * @locale {en} Whether to enable keyboard support
     * @locale {zh} 是否启用键盘支持
     */
    keyboard?: boolean;
    /**
     * @locale {en} Whether to enable drag support
     * @locale {zh} 是否启用拖动支持
     */
    draggable?: boolean;
    /**
     * @locale {en} Custom image caption
     * @locale {zh} 自定义图片标题
     */
    caption?: React.ReactNode;
    /**
     * @locale {en} Whether show images loop
     * @locale {zh} 是否显示图片循环
     */
    loop?: boolean;
    /**
     * @locale {en} Whether to enable close button
     * @locale {zh} 是否启用关闭按钮
     */
    closeable?: boolean | {
        backdrop: boolean;
    };
    /**
     * @locale {en} Whether to show navbar
     * @locale {zh} 是否显示导航栏
     */
    navbar?: boolean;
    /**
     * @locale {en} Whether to show toolbar
     * @locale {zh} 是否显示工具栏
     */
    toolbar?: boolean | {
        /**
         * @locale {en} Custom toolbar config
         * @locale {zh} 自定义工具栏配置
         */
        custom?: ToolbarConfig[];
        /**
         * @locale {en} Whether to show zoom button
         * @locale {zh} 是否显示缩放按钮
         */
        zoomable?: boolean;
        /**
         * @locale {en} Whether to show change button
         * @locale {zh} 是否显示切换按钮
         */
        changeable?: boolean;
        /**
         * @locale {en} Whether to show reset button
         * @locale {zh} 是否显示重置按钮
         */
        resettable?: boolean;
        /**
         * @locale {en} Whether to show scale button
         * @locale {zh} 是否显示镜像按钮
         */
        flippable?: boolean;
        /**
         * @locale {en} Whether to show rotate button
         * @locale {zh} 是否显示旋转按钮
         */
        rotatable?: boolean;
        /**
         * @locale {en} Whether to show download button
         * @locale {zh} 是否显示下载按钮
         */
        downloadable?: boolean;
    };
    /**
     * @locale {en} Whether to show footer
     * @locale {zh} 是否显示底部
     */
    footer?: boolean;
    /**
     * @locale {en} Default image size
     * @locale {zh} 默认图片大小
     */
    defaultSize?: ViewerImageSize;
    /**
     * @locale {en} If load img failed, show default img
     * @locale {zh} 如果加载图片失败，显示默认图片
     */
    defaultImg?: ViewerDefaultImg;
    /**
     * @locale {en} Callback when image change
     * @locale {zh} 图片切换的回调
     */
    onActiveIndexChange?: (index: number, activeImage: ImageDecorator) => void;
    /**
     * @locale {en} Adds a custom class name to the preview root element.
     * @locale {zh} 附加到预览根节点的自定义类名。
     */
    className?: string;
    /**
     * @locale {en} Configures zoom behavior such as default scale, limits, and mouse wheel sensitivity.
     * @locale {zh} 配置缩放行为，例如默认缩放比例、最大最小比例和滚轮缩放速度。
     */
    zoom?: {
        /**
         * @locale {en} Whether to reset zoom after image change
         * @locale {zh} 是否在图片切换后重置缩放
         */
        resetZoomAfterChange?: boolean;
        /**
         * @locale {en} Whether to enable mouse zoom
         * @locale {zh} 是否启用鼠标缩放
         */
        mouseZoom?: boolean;
        /**
         * @locale {en} Default scale
         * @locale {zh} 默认缩放比例
         */
        defaultScale?: number;
        /**
         * @locale {en} Max scale
         * @locale {zh} 最大缩放比例
         */
        maxScale?: number;
        /**
         * @locale {en} Min scale
         * @locale {zh} 最小缩放比例
         */
        minScale?: number;
        /**
         * @locale {en} Zoom speed
         * @locale {zh} 缩放速度
         */
        speed?: number;
    };
}
export default ViewerProps;
