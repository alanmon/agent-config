import type { EventNames, SlotNames, Options, ReactWebComponent } from './lib/create-component';
export type StencilReactComponent<I extends HTMLElement, E extends EventNames = {}, S extends SlotNames = {}> = ReactWebComponent<I, E, S>;
type DefineCustomElement = {
    tagNameTransform: false;
    defineCustomElement: () => void;
} | {
    tagNameTransform: true;
    defineCustomElement: (transTagName: (tagName: string, isDynamic?: boolean) => string) => void;
};
/**
 * Defines a custom element and creates a React component.
 * @public
 */
export declare const createComponent: <I extends HTMLElement, E extends EventNames = {}, S extends SlotNames = {}>({ tagNameTransform, defineCustomElement, ...options }: Options<I, E, S> & DefineCustomElement) => ReactWebComponent<I, E, S>;
export {};
/**
 * Defines a custom element and creates a React component for server side rendering.
 * @public
 */
