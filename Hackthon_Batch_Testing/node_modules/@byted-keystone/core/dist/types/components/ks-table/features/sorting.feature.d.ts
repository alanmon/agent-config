import { type EventEmitter, type MixinFactory } from '../../../stencil-public-runtime';
import { type SortingOptions, type RowData, type SortingState } from '@byted-keystone/table';
import type { TableSortingConfig, TableFeatureArgs } from '../types';
export declare const SortingFactory: (Base: ReturnType<MixinFactory>) => {
    new (): {
        [x: string]: any;
        internalSortingState: SortingState;
        /** Sorting configuration. */
        sorting: TableSortingConfig;
        /** Emitted when the sorting state changes. Emits the next sorting configuration. */
        ksSortingStateChange: EventEmitter<SortingState>;
        getSortingState: (manual: boolean) => SortingState;
        setInitialSortingState: () => void;
        getSortingFeatureOptions: ({ manual, columns, updateTableOptions, }: TableFeatureArgs) => SortingOptions<RowData>;
    };
};
