import { type MixinFactory } from '../../../stencil-public-runtime';
import Sortable from 'sortablejs';
import type { ColumnOrderOptions, ColumnOrderState } from '@byted-keystone/table';
import { Virtualizer } from '@tanstack/virtual-core';
import type { TableFeatureArgs } from '../types';
export declare const ReorderFactory: (Base: ReturnType<MixinFactory>) => {
    new (): {
        [x: string]: any;
        sortable?: Sortable;
        internalColumnOrderState: ColumnOrderState;
        currentDragging: string | null;
        columnVirtualizer?: Virtualizer<HTMLDivElement, HTMLTableCellElement>;
        /** Whether to enable draggable columns. */
        draggable: boolean;
        initializeSortable(el: HTMLElement, options: Sortable.Options): void;
        getColumnOrderState: () => ColumnOrderState;
        setInitialColumnOrderState: ({ columns }: TableFeatureArgs) => void;
        getReorderFeatureOptions: ({ updateTableOptions }: TableFeatureArgs) => ColumnOrderOptions;
    };
};
