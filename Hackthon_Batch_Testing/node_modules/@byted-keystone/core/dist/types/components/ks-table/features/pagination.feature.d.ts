import { type EventEmitter, type MixinFactory } from '../../../stencil-public-runtime';
import { type PaginationState, type PaginationOptions } from '@byted-keystone/table';
import type { TablePaginationConfig, TableFeatureArgs } from '../types';
export declare const PaginationFactory: (Base: ReturnType<MixinFactory>) => {
    new (): {
        [x: string]: any;
        internalPaginationState: PaginationState;
        /** Pagination configuration. Set to false to disable pagination. */
        pagination?: TablePaginationConfig | false;
        /** Emitted when the pagination state changes. Emits the next page index and page size. */
        ksPaginationStateChange: EventEmitter<PaginationState>;
        getPaginationState: (manual: boolean) => PaginationState;
        setInitialPaginationState: () => void;
        getPaginationFeatureOptions: ({ manual, updateTableOptions }: TableFeatureArgs) => PaginationOptions;
    };
};
