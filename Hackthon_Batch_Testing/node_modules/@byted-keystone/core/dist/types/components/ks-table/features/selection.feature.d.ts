import { type EventEmitter, type MixinFactory } from '../../../stencil-public-runtime';
import { DetailArgs } from "../../../shared/dom/index";
import type { Row, RowSelectionOptions, RowData, RowSelectionState } from '@byted-keystone/table';
import type { TableFeatureArgs, TableRowSelectionConfig } from '../types';
export declare const SelectionFactory: (Base: ReturnType<MixinFactory>) => {
    new (): {
        [x: string]: any;
        internalRowSelectionState: RowSelectionState;
        /** Row selection configuration for single or multiple selection. */
        rowSelection?: TableRowSelectionConfig;
        /** Emitted when the row selection state changes. Emits the selection state and selected rows. */
        ksRowSelectionStateChange: EventEmitter<DetailArgs<[RowSelectionState, (Row<RowData> | null)[]]>>;
        assembleRowSelectionState: (selectedRowKeys?: string[]) => Record<string, boolean>;
        getRowSelectionState: (manual: boolean) => RowSelectionState;
        setInitialRowSelectionState: () => void;
        getSelectionFeatureOptions: ({ manual, getTableInstance, updateTableOptions, }: TableFeatureArgs) => RowSelectionOptions<RowData>;
    };
};
