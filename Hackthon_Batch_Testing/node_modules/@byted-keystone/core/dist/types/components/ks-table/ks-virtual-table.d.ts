import { type ComponentInterface, VNode } from '../../stencil-public-runtime';
import { KsTable } from './ks-table';
import { type VirtualTableVirtualizerOptions } from './controllers/virtual-table.controller';
import type { Row, RowData, HeaderGroup, Header, Cell } from '@byted-keystone/table';
import { type Virtualizer } from '@tanstack/virtual-core';
import type { TableColumn, TableColumnGroup, TableFrameMode, TableDynamicRenderMode, TableRowPinningConfig, TableExpandableConfig, TableScrollToPositionOptions } from './types';
/**
 * @slot append - Content to display at the end of the table.
 * @slot empty - Content to display when the table has no data.
 * @part base - The base container of the component.
 */
export declare class KsVirtualTable extends KsTable implements ComponentInterface {
    private rowVirtualizer?;
    private bodyCellDynamicSlotCache;
    private readonly virtualController;
    columnVirtualizer?: Virtualizer<HTMLDivElement, HTMLTableCellElement>;
    /** Key generator for slot key, default to use cell or header id. */
    slotKey: (instance: Header<RowData, unknown> | Cell<RowData, unknown>) => string;
    /** Estimated height (px) of each row in the list, applied to the height of the row skeleton during loading. */
    estimatedRowHeight?: number | ((index: number) => number);
    /** Whether the table frame fills the available height or follows content height. */
    frameMode: TableFrameMode;
    /** virtualizer configuration */
    virtualizerOptions?: VirtualTableVirtualizerOptions;
    /** Dynamic render mode for virtual rows. */
    dynamicRenderMode: TableDynamicRenderMode;
    protected onInternalExpandedStateChange(): void;
    protected expandableChanged(expandable: TableExpandableConfig): void;
    protected columnsChanged(columns: (TableColumn<RowData> | TableColumnGroup)[]): void;
    protected rowPinningChanged(cfg: TableRowPinningConfig, oldCfg: TableRowPinningConfig): void;
    protected dataSourceChanged(dataSource: RowData[]): void;
    protected onResponsiveColumnWidthsChanged(): void;
    protected getCssVariables: () => {
        [key: string]: string | undefined;
    };
    componentWillLoad(): void;
    componentWillRender(): void;
    protected updateDynamicSlots: () => void;
    protected getDynamicSlots({ rows, headerGroups, virtualColumnIndexes, includeBodyCells, }: {
        rows: Row<RowData>[];
        headerGroups: HeaderGroup<RowData>[];
        virtualColumnIndexes: number[];
        includeBodyCells: boolean;
    }): unknown[];
    private clearBodyCellDynamicSlotCache;
    private handleHeadRowAction;
    private handleHeaderDynamicRenderDeps;
    private handleMeasureRow;
    protected onWrapperScroll: EventListener;
    protected scrollToTablePosition(options?: TableScrollToPositionOptions): void;
    protected renderSkeletonRows(): any[];
    render(): any;
    protected passThroughSlots: Record<string, VNode[]>;
    protected headerDynamicRenderDeps: Record<string, unknown[]>;
    protected __DEPS__(): any[];
}
