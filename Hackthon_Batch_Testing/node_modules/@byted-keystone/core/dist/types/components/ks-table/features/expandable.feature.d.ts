import { type EventEmitter, type MixinFactory } from '../../../stencil-public-runtime';
import { type Row, type ExpandedOptions, type RowData, type ExpandedState } from '@byted-keystone/table';
import { DetailArgs } from "../../../shared/dom/index";
import type { TableExpandableConfig, TableFeatureArgs } from '../types';
export declare const ExpandableFactory: (Base: ReturnType<MixinFactory>) => {
    new (): {
        [x: string]: any;
        internalExpandedState: ExpandedState;
        /** Expandable row configuration. Enables and customizes row expansion. */
        expandable?: TableExpandableConfig;
        /** Emitted when the expanded row state changes. Emits the expanded state, expanded rows, and the row that changed. */
        ksExpandedStateChange: EventEmitter<DetailArgs<[ExpandedState, Row<RowData>[], Row<RowData> | null]>>;
        assembleExpandedState: (expandedRowKeys?: string[]) => Record<string, boolean>;
        getExpandedState: (manual: boolean) => ExpandedState;
        setInitialExpandedState: () => void;
        getExpandableFeatureOptions: ({ manual, getTableInstance, updateTableOptions, }: TableFeatureArgs) => ExpandedOptions<RowData>;
    };
};
