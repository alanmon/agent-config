import { MenuItemBase } from "../ks-menu-item/types";
import { JSXBase } from '../../stencil-public-runtime';
export interface DropdownItem extends MenuItemBase<DropdownItem> {
    checked?: boolean;
    halfChecked?: boolean;
    hasPopup?: boolean;
    multiple?: boolean;
    highlight?: [number, number];
    hideCheckmark?: boolean;
    children?: DropdownItem[];
    onMouseEnter?: JSXBase.DOMAttributes<HTMLLIElement>['onMouseEnter'];
    onMouseLeave?: JSXBase.DOMAttributes<HTMLLIElement>['onMouseLeave'];
    classes?: {
        [className: string]: boolean;
    };
    dataAttrs?: Record<`data-${string}`, string | boolean>;
}
