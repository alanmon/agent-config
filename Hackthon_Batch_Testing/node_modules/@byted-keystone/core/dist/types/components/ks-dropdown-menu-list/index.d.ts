import { EventEmitter, VNode } from '../../stencil-public-runtime';
import { DetailArgs } from "../../shared/dom/index";
import { Value } from "../ks-value-field/types";
import { RenderConfig } from "../ks-virtual-list/types";
import { KsVirtualList } from "../ks-virtual-list/index";
import { FlattenNode } from "../ks-tree-view/utils/index";
import { DropdownItem } from './types';
/**
 * @slot listHeader - Slot for custom header at the top of the list (scrollable area in DropdownMenu).
 * @slot empty - Slot for custom content when the list is empty.
 * @slot loadFailure - Slot for custom content when the list fails to load.
 * @slot loadMoreFailure - Slot for custom content when the list fails to load more items.
 */
export declare class KsDropdownMenuList extends KsVirtualList<FlattenNode<DropdownItem>> {
    el: HTMLKsDropdownMenuListElement;
    protected prefixCls: string;
    protected items: FlattenNode<DropdownItem>[];
    protected renderItem(item: FlattenNode<DropdownItem>, index: number): VNode;
    protected renderDynamicChildrenForItem(item: FlattenNode<DropdownItem>, index: number, wrapWithSlot: (slotName: string, children: unknown) => unknown, renderConfig?: RenderConfig): unknown[];
    protected getPassThroughSlots: (item: FlattenNode<DropdownItem>, index: number) => VNode[];
    protected DEFAULT_HEIGHT: (item: FlattenNode<DropdownItem>) => 36 | 54;
    /** The list of items to be displayed. */
    options: DropdownItem[];
    /** A function to filter the options. */
    filterOption?: (node: DropdownItem) => boolean;
    /** A function to sort the options. */
    sortOption?: (a: DropdownItem, b: DropdownItem) => number;
    /** The search value of the dropdown menu list. */
    searchValue?: string;
    filteredOptions?: DropdownItem[];
    generateFilteredOptions(): void;
    /** Whether to enable virtualization. */
    virtual?: boolean | {
        estimatedItemHeight: number | ((item: FlattenNode<DropdownItem>) => number);
    };
    /** Indicates whether the dropdown menu list is loading. */
    loading?: boolean;
    /** Indicates whether the dropdown menu list is loading more items. */
    loadingMore?: boolean;
    /** Indicates whether the dropdown menu list failed to load. */
    loadFailed?: boolean;
    /** Indicates whether the dropdown menu list failed to load more items. */
    loadMoreFailed?: boolean;
    /** Whether to support selectable items. */
    selectable: boolean;
    /** Whether to support multiple selection. */
    multiple: boolean;
    /** Indicates whether the dropdown menu list is disabled. */
    disabled?: boolean;
    /** The selected value(s) of the dropdown menu list. */
    value?: Value[];
    /** The half selected value(s) of the dropdown menu list. */
    halfValues?: Value[];
    /** Whether to hide the divider between groups of items. */
    hideGroupDivider: boolean;
    /** Whether to hide the title of the search result. */
    hideSearchResultTitle: boolean;
    /** The slot at the top of the list (scrollable area in DropdownMenu). */
    private listHeaderSlots?;
    /** The slot to be displayed when the list is empty. */
    /** The slot to be displayed when the list fails to load. */
    /** The slot to be displayed when the list fails to load more items. */
    /** Triggered when the value of the dropdown menu list changes. */
    ksChange: EventEmitter<DetailArgs<[
        Value[],
        {
            value: Value;
        }
    ]>>;
    private handleItemClick;
    handleFilteredOptionsChange(): void;
    connectedCallback(): void;
    disconnectedCallback(): void;
    componentWillLoad(): Promise<void> | void;
    componentDidRender(): void;
    private renderEmpty;
    private renderLoadFail;
    private renderSearchResultTitle;
    render(): any;
    /** @private Internal prop for render layer, please don't use. */
    wrapWithSlot: (slotName: string, children: unknown) => unknown;
    /** @private Internal prop for render layer, please don't use. */
    setDynamicRenderDeps: (children: unknown[], getPassThroughSlots?: () => VNode[]) => void;
    protected __DEPS__(): any[];
}
