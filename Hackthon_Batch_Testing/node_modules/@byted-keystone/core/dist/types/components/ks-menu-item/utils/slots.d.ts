import { VNode } from '../../../stencil-public-runtime';
import { MenuItemBase, Value } from '../types';
import { RenderConfig } from "../../ks-virtual-list/types";
/** Get the slot name for a menu item. */
export declare const getItemSlotNameFromSlotTypeAndValue: (slotType: string, itemValue: Value, ...restValues: Value[]) => string;
/**
 * 在渲染菜单项目时，根据项目值和其他值生成插槽名称，用于渲染菜单项目内部的插槽。
 * <MenuItemBase>
 *    {...renderItemSlots(item, ...restValues)}
 * </MenuItemBase>
 * Render the slots for a menu item.
 */
export declare const renderItemSlots: (item: MenuItemBase, ...restValues: Value[]) => VNode[];
/** Render the dynamic children for a menu item. */
export declare const renderDynamicChildren: <T extends MenuItemBase<T>>(item: T, index: number, wrapWithSlot: (slotName: string, children: unknown) => unknown, renderConfig?: RenderConfig) => unknown[];
/**
 * 在渲染菜单项目时，根据 item 渲染出透传的插槽数组
 * <ks-xxx>
 *   <MenuItemBase>
 *      {...renderItemSlots(item, ...restValues)}
 *   </MenuItemBase>
 *   {...renderPassThroughSlots(item, ...restValues)}
 * </ks-xxx>
 * Render the slots for a menu item.
 */
export declare const renderPassThroughSlots: <T extends MenuItemBase<T>>(item: T, ...restValues: Value[]) => VNode[];
