import { VNode } from '../../stencil-public-runtime';
import { MenuItemBaseProps } from './item';
import { TreeDataNode } from '@src/components';
export interface TreeItemProps extends MenuItemBaseProps<TreeDataNode> {
    classes?: {
        [className: string]: boolean;
    };
    loading?: boolean;
    indent?: number;
    expanded?: boolean;
    onExpand?: (e: PointerEvent) => void;
}
export declare const KsTreeItem: ({ classes, indent, expanded, onExpand, loading, ...rest }: TreeItemProps, children: VNode[]) => any;
