export type TimelineStatus = 'default' | 'error';
export type TimelineRenderable = string | (() => unknown);
export interface TimelineItem {
    disabled?: boolean;
    status?: TimelineStatus;
    title?: TimelineRenderable;
    description?: TimelineRenderable;
}
