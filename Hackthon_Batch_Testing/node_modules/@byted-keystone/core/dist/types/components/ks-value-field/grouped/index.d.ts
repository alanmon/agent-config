import { ComponentInterface, EventEmitter } from '../../../stencil-public-runtime';
import { ValueFieldGroup, ValueFieldInputSize } from '../types';
/**
 * @part input - The native input element.
 */
export declare class KsGroupedValueField implements ComponentInterface {
    el: HTMLKsGroupedValueFieldElement;
    contentEl: HTMLDivElement;
    /** Render value */
    value: ValueFieldGroup[];
    /** Size of the tag. */
    size?: ValueFieldInputSize;
    /** Whether to show close button of tags. */
    closeable: boolean;
    /** Whether the fields presenter is disabled. If `true`, interaction is not allowed. */
    disabled?: boolean;
    /** The height of the value field. */
    valueFieldHeight: string | number | {
        min?: string | number;
        max?: string | number;
    };
    /**
     * @private
     * The inline-end offset reserved for the suffix area.
     */
    suffixOffset: number;
    /** Placeholder of the input field. */
    placeholder?: string;
    /** Emitted when the close button of tag is clicked. */
    ksTagClose: EventEmitter<{
        groupValue: string | number;
        value: string | number;
    }>;
    get containerStyle(): {
        [key: string]: string | undefined;
    };
    private renderGroupedItem;
    render(): any;
}
