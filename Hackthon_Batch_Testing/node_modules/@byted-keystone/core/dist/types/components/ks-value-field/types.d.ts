import { Components } from '@src/components';
import { Status } from "../../shared/base-props/index";
import { CSSProperties } from "../../shared/dom/index";
export type ValueFieldInputSize = 'lg' | 'md' | 'sm';
export type Value = string | number;
export interface ValueField {
    value: Value;
    label?: string;
    disabled?: boolean;
    status?: Status;
    closeable?: boolean;
    style?: CSSProperties;
    popover?: Partial<ValueFieldTagPopoverProps>;
}
export type ValueFieldTagPopoverProps = Omit<Components.KsTooltip, // FIXME switch to KsPopover
// FIXME switch to KsPopover
'anchorEl' | 'close' | 'getContainer' | 'open' | 'visible' | 'defaultVisible' | 'trigger'>;
export interface ValueFieldGroup {
    value: Value;
    label?: string;
    disabled?: boolean;
    children: ValueField[];
}
export interface RenderConfig {
    collapsed?: boolean;
}
export type RenderField = ValueField & RenderConfig;
export type RenderFieldGroup = Omit<ValueFieldGroup, 'children'> & {
    children: RenderField[];
};
export type RenderValue = RenderField | RenderFieldGroup;
export type ValueFieldFreeSoloConfig = boolean | {
    valueFieldFromInput: (inputValue: string) => ValueField;
};
