import { ComponentInterface, EventEmitter } from '../../../stencil-public-runtime';
import { ValueField, ValueFieldInputSize } from '../types';
/**
 * @part input - The native input element.
 * @part mirror
 */
export declare class KsTextedValueField implements ComponentInterface {
    el: HTMLKsTextedValueFieldElement;
    private inputEl?;
    private inputMirrorEl?;
    private inputWidthObserver?;
    /** Size of the text. */
    size: ValueFieldInputSize;
    /** Render value */
    value: ValueField[] | string;
    /** Whether the fields presenter is disabled. If `true`, interaction is not allowed. */
    disabled?: boolean;
    /**
     * @private
     * The inline-end offset reserved for the suffix area.
     */
    suffixOffset: number;
    /** The height of the value field. */
    valueFieldHeight: string | number | {
        min?: string | number;
        max?: string | number;
    };
    /** Whether to enable input. */
    enableInput?: boolean;
    /** Placeholder of input. */
    placeholder?: string;
    /** Input value. */
    inputValue?: string;
    /** Whether the value field element has focus. */
    focusElement?: boolean;
    /** Emitted when the input value is changed. */
    ksInputChange: EventEmitter<string>;
    _selectionStart: number;
    _selectionEnd: number;
    _currentInputValue: string;
    _isComposing: boolean;
    private handleInput;
    private handleCompositionStart;
    private handleCompositionEnd;
    private setInputRef;
    private syncSelectionFromInput;
    private restoreSelection;
    private handleInputClick;
    get containerStyle(): {
        [key: string]: string | undefined;
    };
    onFocusElement(): void;
    render(): any;
    private getValueText;
    private shouldShowInteractiveInput;
    private isDisplayPlaceholder;
    private getDisplayText;
    private getMirrorText;
    private getInputValue;
    componentDidRender(): void;
    private syncInputWidth;
    syncInputWidthOnValueChange(): void;
    syncInputValueFromProp(): void;
    componentWillLoad(): Promise<void> | void;
    disconnectedCallback(): void;
}
