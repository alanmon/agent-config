import { EventEmitter, ComponentInterface } from '../../../stencil-public-runtime';
import { RenderField, ValueField } from '../types';
/**
 * @part tagItem
 * @part input - The native input element.
 */
export declare class KsTaggedValueField implements ComponentInterface {
    el: HTMLKsTaggedValueFieldElement;
    private contentEl?;
    private inputEl?;
    private inputMirrorEl?;
    private inputWidthObserver?;
    /** Render value */
    value: ValueField[];
    /** Whether to collapse when tags overflow. */
    tagsEllipsis: boolean;
    /** Size of the tag. */
    /** Whether to show close button of tags. */
    closeable: boolean;
    /** Whether the fields presenter is disabled. If `true`, interaction is not allowed. */
    disabled?: boolean;
    /** The height of the value field. */
    valueFieldHeight: string | number | {
        min?: string | number;
        max?: string | number;
    };
    /**
     * @private
     * The inline-end offset reserved for the suffix area.
     */
    suffixOffset: number;
    /** Whether to enable input. */
    enableInput?: boolean;
    /** Placeholder of input. */
    placeholder?: string;
    /** Input value. */
    inputValue?: string;
    /** Whether the value field element has focus. */
    focusElement?: boolean;
    /** Emitted when the close button of tag is clicked. */
    ksTagClose: EventEmitter<string | number>;
    /** Emitted when the input value is changed. */
    ksInputChange: EventEmitter<string>;
    /** Emitted when a key is pressed down in the input. */
    ksInputKeyDown: EventEmitter<KeyboardEvent>;
    get contentStyle(): {
        [key: string]: string | undefined;
    };
    _renderValue: RenderField[];
    _moreCount: number;
    _wrap: boolean;
    componentWillLoad(): Promise<void> | void;
    updateRenderValue(value: ValueField[]): void;
    private collapsedTagStyle;
    private getMoreTagWidth;
    private updateMore;
    _selectionStart: number;
    _selectionEnd: number;
    _currentInputValue: string;
    private composition;
    private handleInput;
    private handleCompositionStart;
    private handleCompositionEnd;
    private syncSelectionFromInput;
    private restoreSelection;
    private handleContainerClick;
    private handleInputClick;
    private handleInputKeyDown;
    private handleInputFocus;
    private setInputRef;
    private shouldShowInputText;
    private isEditing;
    private shouldShowDisplayPlaceholder;
    private isEmptyTailDisplayState;
    private shouldOffsetInputText;
    private getInputValue;
    private syncInputWidth;
    onInputValueChange(): void;
    onPlaceholderChange(): void;
    componentDidRender(): void;
    render(): any;
    private getMirrorText;
    disconnectedCallback(): void;
}
