export declare class HitBottomObserver {
    private onHitBottom;
    private observer?;
    private root?;
    private target?;
    private observingRoot?;
    private observingTarget?;
    constructor(onHitBottom: () => void);
    setRoot(root?: HTMLDivElement): void;
    setTarget(target?: HTMLDivElement): void;
    disconnect(): void;
    private sync;
}
