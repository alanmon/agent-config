import { ComponentInterface } from '../../stencil-public-runtime';
import { PartialKeys, Virtualizer, VirtualizerOptions } from '@tanstack/virtual-core';
type HostComponent = ComponentInterface & {
    el: HTMLElement;
} & {
    isConnected?: boolean;
};
declare class VirtualizerControllerBase<TScrollElement extends Element | Window, TItemElement extends Element> {
    host: HostComponent;
    private readonly virtualizer;
    private cleanup?;
    constructor(host: HostComponent, options: VirtualizerOptions<TScrollElement, TItemElement>);
    getVirtualizer(): Virtualizer<TScrollElement, TItemElement>;
}
export declare class VirtualizerController<TScrollElement extends Element, TItemElement extends Element> extends VirtualizerControllerBase<TScrollElement, TItemElement> {
    constructor(host: HostComponent, options: PartialKeys<VirtualizerOptions<TScrollElement, TItemElement>, 'observeElementRect' | 'observeElementOffset' | 'scrollToFn'>);
}
export {};
