import { KsTooltip } from "../ks-tooltip/index";
import { CSSProperties } from "../../shared/dom/index";
import { DropdownItem } from "../ks-dropdown-menu-list/types";
export interface DropdownPopoverProps {
    delay?: {
        open?: number;
        close?: number;
    };
    gapOffset?: number;
    startOffset?: number;
    trigger?: KsTooltip['trigger'];
}
export interface DropdownSearchConfig {
    placeholder?: string;
    autoClearSearchValue?: boolean;
    hideSearchInput?: boolean;
    hideSearchResultTitle?: boolean;
    filter?: (searchQuery: string, option: DropdownItem) => boolean;
    sort?: (a: DropdownItem, b: DropdownItem) => number;
}
export type CustomRenderFn = () => HTMLElement | Element | number | string | unknown;
export interface DropdownMenuPartProps {
    tooltip?: Partial<KsTooltip>;
    popup?: {
        style?: CSSProperties;
    } & Record<string, unknown>;
}
