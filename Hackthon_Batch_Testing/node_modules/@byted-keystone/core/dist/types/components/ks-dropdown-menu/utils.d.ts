import { DropdownItem } from "../ks-dropdown-menu-list/types";
import { CustomRenderFn } from './types';
import { VNode } from '../../stencil-public-runtime';
/** Default filter function for dropdown menu search. */
export declare const defaultFilter: (searchQuery: string, option: DropdownItem) => boolean;
/** Filter data attributes from the given attributes. */
export declare const filterDataAttrs: (attrs: Record<string, string | boolean>) => Record<`data-${string}`, string | boolean>;
/** Get the slot name for a dropdown menu tab. */
export declare const getTabSlotNameFromSlotType: (slotType: string, tabId?: string) => string;
/**
 * Render the slots for a dropdown menu tab.
 * Note: This only render tabs related slots. If you want to render item related slots, please also use `internalRenderDynamicSlotsForItems`.
 */
export declare const internalRenderDynamicSlotsForTabs: (props: {
    tabId: string;
    label?: string | CustomRenderFn;
    helpContent?: string | CustomRenderFn;
}, wrapWithSlot: (slotName: string, children: unknown) => unknown) => unknown[];
export declare const internalRenderPassThroughSlotsForTabs: (props: {
    tabId: string;
    label?: string | CustomRenderFn;
    helpContent?: string | CustomRenderFn;
}) => VNode[];
