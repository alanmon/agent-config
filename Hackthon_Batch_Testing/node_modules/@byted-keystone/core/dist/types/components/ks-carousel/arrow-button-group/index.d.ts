import { EventEmitter } from '../../../stencil-public-runtime';
import { ArrowPaginationValue } from '../types';
export declare class KsArrowButtonGroup {
    el: HTMLKsArrowButtonGroupElement;
    /** Show the component; triggers the enter or exit states */
    currentIndex: number;
    count: number;
    slidesToShow: number;
    infinite: boolean;
    orient: 'left' | 'right';
    disabled: boolean;
    arrows: boolean | 'overlay';
    bulletinMode: boolean;
    ksChange: EventEmitter<ArrowPaginationValue>;
    ksArrowOver: EventEmitter;
    ksArrowLeave: EventEmitter;
    ksArrowEnter: EventEmitter;
    render(): any;
}
