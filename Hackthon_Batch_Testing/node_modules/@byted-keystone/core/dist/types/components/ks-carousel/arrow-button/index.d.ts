import { EventEmitter } from '../../../stencil-public-runtime';
import { ArrowPaginationValue } from '../types';
export declare class KsArrowButton {
    el: HTMLKsArrowButtonElement;
    /** Show the component; triggers the enter or exit states */
    currentIndex: number;
    count: number;
    slidesToShow: number;
    infinite: boolean;
    orient: 'left' | 'right';
    disabled: boolean;
    ksChange: EventEmitter<ArrowPaginationValue>;
    render(): any;
}
