import { EventEmitter } from '../../../stencil-public-runtime';
import { ArrowPaginationValue } from '../types';
export declare class KsCarouselArrowPagination {
    el: HTMLKsCarouselArrowPaginationElement;
    /** Show the component; triggers the enter or exit states */
    currentIndex: number;
    count: number;
    slidesToShow: number;
    infinite: boolean;
    ksChange: EventEmitter<ArrowPaginationValue>;
    ksArrowOver: EventEmitter;
    ksArrowLeave: EventEmitter;
    ksArrowEnter: EventEmitter;
    render(): any;
}
