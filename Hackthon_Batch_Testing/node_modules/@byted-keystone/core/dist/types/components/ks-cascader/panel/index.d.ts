import { EventEmitter, ComponentInterface } from '../../../stencil-public-runtime';
import { Value } from "../../ks-menu-item/types";
import { ShowCheckedStrategy, SingleValueType, CascaderDataNode, CascaderSearchNode, CascaderMultiple } from '../types';
import { DetailArgs } from "../../../shared/dom/index";
export declare class KsCascaderPanel implements ComponentInterface {
    el: HTMLKsCascaderPanelElement;
    options: CascaderDataNode[];
    searchOptions: CascaderSearchNode[];
    disabled?: boolean;
    multiple: CascaderMultiple;
    private get mergedMultiple();
    private get multipleConfig();
    showCheckedStrategy: ShowCheckedStrategy;
    value?: SingleValueType | SingleValueType[];
    defaultValue?: SingleValueType | SingleValueType[];
    changeOnSelect?: boolean;
    loadData?: (selectOptions: CascaderDataNode[]) => Promise<void> | void;
    loadedKeys?: Value[][];
    searchValue: string;
    toggleOpen: (open?: boolean) => void;
    open?: boolean;
    internalValueState?: SingleValueType | SingleValueType[];
    ksChange: EventEmitter<DetailArgs<[SingleValueType | SingleValueType[], CascaderDataNode[] | CascaderDataNode[][]]>>;
    ksLoad: EventEmitter<Value[][]>;
    private pathKeyEntities;
    transformOptionsToEntities(): void;
    handleValueStateChange(): void;
    /**
     * this is a state to store checked keys and half checked keys
     *
     * - checkedKeys: keys that are checked
     * - halfCheckedKeys: keys that are half checked
     *
     * We do not need to decorate it with `@State()` because it is updated in
     * watcher function for internalValueState. So we can ensure that it is
     * **always** up-to-date.
     */
    private checkedState;
    private onSelect;
    private onSelectAll;
    private triggerChange;
    private setPanelListProps;
    render(): any;
    componentWillLoad(): void;
    /**
     * @private */
    wrapWithSlot: (slotName: string, children: unknown) => unknown;
    /**
     * @private */
    setDynamicRenderDeps: (children: unknown[]) => void;
}
