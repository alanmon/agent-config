import { type PaginationItem } from './helpers';
export declare const getTotalPageCount: (total: number, pageSize: number) => number;
export declare const getPaginationWindowSize: (siblingCount: number) => number;
export declare const getVisiblePaginationItemCount: (siblingCount: number) => number;
/**
 * Generates a list of pagination items with ellipsis for large page ranges.
 * The function creates a pagination list that shows:
 * - First page
 * - Last page
 * - A window of pages around the current page
 * - Ellipsis (...) when there are gaps in the sequence
 *
 * @param visiblePagesAroundCurrent - Number of pages to show on each side of the current page
 * @param totalPages - Total number of pages
 * @param currentPage - Current active page (1-based)
 * @returns Array of pagination items including numbers and ellipsis
 */
export declare const getPaginationItemList: ({ siblingCount: visiblePagesAroundCurrent, currentPage, totalPageCount, }: {
    siblingCount: number;
    currentPage: number;
    totalPageCount: number;
}) => PaginationItem[];
