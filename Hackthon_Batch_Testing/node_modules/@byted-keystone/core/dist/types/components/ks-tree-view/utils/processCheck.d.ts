import { TreeDataNode } from '../types';
import { DataEntity, Value } from './interface';
interface CheckResult {
    checkedKeys: Value[];
    halfCheckedKeys: Value[];
    key2CheckedChildrenCount: Map<Value, number>;
}
export declare function processCheck<TreeDataType extends TreeDataNode = TreeDataNode>(keyList: Value[], checked: true | {
    checked: false;
    halfCheckedKeys: Value[];
}, keyEntities: Record<Value, DataEntity<TreeDataType>>, config?: {
    getDisabled?: (node: TreeDataType) => boolean;
    getKey?: (node: DataEntity<TreeDataType>) => Value;
}): CheckResult;
export {};
