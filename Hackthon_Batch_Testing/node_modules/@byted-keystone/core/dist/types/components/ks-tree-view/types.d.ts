import { KsTooltip } from "../ks-tooltip/index";
import type { CSSProperties } from "../../shared/dom/index";
import type { MenuItemBase } from "../ks-menu-item/types";
export interface TreeDataNode extends MenuItemBase<TreeDataNode> {
    classes?: {
        [className: string]: boolean;
    };
    isLeaf?: boolean;
    children?: TreeDataNode[];
    dataAttrs?: Record<`data-${string}`, string | boolean>;
}
export interface TreeSelectPartProps {
    tooltip?: Partial<KsTooltip>;
    popup?: {
        style?: CSSProperties;
    } & Record<string, unknown>;
}
export interface SearchConfig {
    filter?: (inputValue: string, options: TreeDataNode) => boolean;
    autoClearSearchValue?: boolean;
}
