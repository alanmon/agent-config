import { TreeDataNode } from '../types';
import { DataEntity, Value } from './interface';
export declare function processExpandParent(keyList: Value[], keyEntities: Record<Value, DataEntity<TreeDataNode>>): Value[];
