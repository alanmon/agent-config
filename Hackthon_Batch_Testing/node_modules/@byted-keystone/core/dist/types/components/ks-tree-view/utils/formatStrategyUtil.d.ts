import { DataEntity, ShowCheckedStrategy, Value } from './interface';
export declare function formatStrategyValues(values: Value[], strategy: ShowCheckedStrategy, keyEntities: Record<Value, DataEntity>): Value[];
