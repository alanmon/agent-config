import { EventEmitter, ComponentInterface } from '../../stencil-public-runtime';
import { IUploadOption } from './utils';
import { BeforeUploadHook, UploadMaxCountConfig } from './types';
export declare class KsRcUpload implements ComponentInterface {
    el: HTMLKsRcUploadElement;
    private fileInput?;
    private reqs;
    accept: string;
    action: string;
    data: object;
    name: string;
    disabled: boolean;
    multiple: boolean;
    headers: object;
    method: string;
    customRequest: (option: IUploadOption) => void;
    beforeUpload?: BeforeUploadHook;
    onstart: (event: CustomEvent) => void;
    onfail: (event: CustomEvent) => void;
    onpro: (event: CustomEvent) => void;
    onsuccess: (event: CustomEvent) => void;
    maxCount?: UploadMaxCountConfig;
    fileListLength: number;
    ksChange: EventEmitter;
    private children;
    handleUpdate(): void;
    private uploadFiles;
    private processFile;
    private post;
    /** Internal */
    abort(file?: any): Promise<void>;
    componentWillLoad(): void;
    private onTrigger;
    private onFileDrop;
    private uploadChange;
    /** Internal */
    clearInputValue(): Promise<void>;
    render(): any;
}
