import { Status } from "../../shared/base-props/index";
export { FormContextValueReconcile } from './FormContextValueReconcile';
/** Mount click event handlers on elements */
export declare function mountFunctionOnClick(elements: HTMLElement[], func: (e: Event) => void): () => void;
/** Get buttons with submit attribute under form */
export declare function getSubmitBtns(node: HTMLElement): HTMLElement[];
/** Get buttons with reset attribute under form */
export declare function getResetBtns(node: HTMLElement): HTMLElement[];
/** Get */
export declare function getReconciledFormContextData(node: {
    _provider?: 'formItem' | 'formList' | 'form';
    disabled?: boolean;
    status?: Status;
    disabledFromContext?: boolean;
    statusFromContext?: Status;
}): {
    disabled: boolean | undefined;
    status: Status | undefined;
};
/**
 * Moves an array item from one position in an array to another.
 *
 * Note: This is a pure function so a new array will be returned, instead
 * of altering the array argument.
 *
 * @param array         Array in which to move an item.         (required)
 * @param moveIndex     The index of the item to move.          (required)
 * @param toIndex       The index to move item at moveIndex to. (required)
 */
export declare function move<T>(array: T[], moveIndex: number, toIndex: number): T[];
/**
 * Parse lodash-style path string
 * Parse lodash-style path string
 */
export declare function parsePath(pathStr: string): string[];
/**
 * Immutably set deep property value of an object
 * Immutably set deep property value of an object
 */
export declare const immutableSet: <T>(originalValue: T, path: string | string[], valueToSet: unknown, defaultValue?: T) => T;
/**
 * Get all paths in an object, each path is a lodash-style array
 * @param obj The object to analyze
 * @returns Array containing all paths, excluding the root path
 */
export declare function getAllPaths(obj: Readonly<object>, excludes?: RegExp): Array<string[]>;
