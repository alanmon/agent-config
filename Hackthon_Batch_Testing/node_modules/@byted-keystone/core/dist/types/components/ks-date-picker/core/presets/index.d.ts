import { ComponentInterface } from '../../../../stencil-public-runtime';
import { Dayjs } from 'dayjs';
import { DateComparisonPresetShortcut, DatePickerViewMode, DatePresetOptions, DatePresetShortcut, DatePresetShortcutStrict, NormalizedDatePickerComparisonProps } from '../../types';
import { Placement } from '@floating-ui/dom';
import { TimezoneInfo } from "../../../../shared/timezone/index";
/**
 * The DateTimePickerBase component serves two primary purposes:
 * 1. Normalization: It normalizes external prop values into a consistent internal format for use by internal components.
 * 2. Composition: It assembles and orchestrates the various internal components (e.g., Calendar, TimePanel) that constitute the complete date-time picker.
 */
export declare class KsDateTimePickerPresets implements ComponentInterface {
    el: HTMLKsDateTimePickerPresetsElement;
    value: [Dayjs | null, Dayjs | null];
    timezone: TimezoneInfo;
    /** If true, the presenter will display two input fields for a range. If false, a single input field is displayed. */
    isRange: boolean;
    now: Dayjs;
    /** If true, the input field(s) will be disabled and non-interactive. */
    disabled: [boolean, boolean];
    comparison: NormalizedDatePickerComparisonProps;
    /** If true, a clear button is shown when the input has a value and is focused, allowing the user to clear the content. */
    open: boolean;
    defaultOpen: boolean;
    setValue: (value: [Dayjs | null, Dayjs | null], preset: {
        key?: DatePresetShortcutStrict;
    }) => void;
    /** The placement of the popover relative to the picker input. */
    placement: Placement;
    availableViewModes: DatePickerViewMode[];
    presets: true | {
        options: DatePresetOptions;
    };
    overridePreset?: DatePresetShortcut[];
    isSelectingComparisonValue: boolean;
    setComparison: (comparison: NormalizedDatePickerComparisonProps, preset: {
        key?: DateComparisonPresetShortcut;
    }) => void;
    viewMode: DatePickerViewMode;
    handleViewChange: (viewDate: Dayjs, viewMode: DatePickerViewMode) => void;
    disabledDate: (date: Dayjs, info: {
        type: 'date' | 'preset';
        from?: Dayjs;
        to?: Dayjs;
        selectionPhase?: 'start' | 'end';
    }) => boolean;
    setPresetPreviewDate: (previewDate: [Dayjs | null, Dayjs | null] | null) => void;
    normalizedFormat: string;
    get locale(): "id" | "ar" | "cs-CZ" | "de" | "en" | "es" | "fr" | "it" | "ja" | "ko" | "ms" | "pl-PL" | "pt-BR" | "pt" | "ru" | "sv-SE" | "th" | "tr" | "vi" | "zh" | "el-GR" | "et" | "bg" | "lv" | "lt" | "sl" | "hr" | "sk" | "nl-NL" | "en-GB" | "es-419";
    private getLastNDaysPreset;
    private getLastNWeeksPreset;
    private getLastNMonthsPreset;
    private getQuickOptionPreset;
    render(): any;
}
