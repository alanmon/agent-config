import { ComponentInterface } from '../../../../stencil-public-runtime';
import { DatePickerViewMode } from '../../types';
import type { Dayjs } from 'dayjs';
import { DateGridSharedContract } from './types';
export declare class KsDateTimePickerDateGrid implements DateGridSharedContract, ComponentInterface {
    el: HTMLKsDateTimePickerDateGridElement;
    viewMode: DatePickerViewMode;
    viewDate: Dayjs;
    availableViewModes: DatePickerViewMode[];
    /** If true, the presenter will display two input fields for a range. If false, a single input field is displayed. */
    isRange: boolean;
    value: [Dayjs | null, Dayjs | null];
    now: Dayjs;
    disabledDate: (date: Dayjs, info: {
        type: 'date' | 'preset';
        from?: Dayjs;
        to?: Dayjs;
        selectionPhase?: 'start' | 'end';
    }) => boolean;
    disabled: [boolean, boolean];
    presetPreviewDate: [Dayjs | null, Dayjs | null] | null;
    displayMode: 'single' | 'dual';
    selectionPhase: 'start' | 'end' | 'idle';
    handleValueChange: (value: [Dayjs | null, Dayjs | null]) => void;
    handleViewChange: (viewDate: Dayjs, viewMode: DatePickerViewMode) => void;
    handleHoverPartChange: (hoverPart: 'start' | 'end' | null) => void;
    private hoverValue;
    private get viewModeConfig();
    private getInlineEndDateGridViewDate;
    private handleHoverValueChange;
    render(): any;
}
