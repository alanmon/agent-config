import { ComponentInterface } from '../../../../../stencil-public-runtime';
import { DatePickerViewMode } from '../../../types';
import type { Dayjs } from 'dayjs';
import { DateGridSharedContract } from '../types';
export declare class KsDateTimePickerDateGridHeader implements Omit<DateGridSharedContract, 'isRange' | 'value' | 'now' | 'selectionPhase' | 'disabledDate' | 'handleValueChange'>, ComponentInterface {
    el: HTMLKsDateTimePickerDateGridHeaderElement;
    viewDate: Dayjs;
    viewMode: DatePickerViewMode;
    availableViewModes: DatePickerViewMode[];
    layoutMode: 'inline-start' | 'inline-end' | 'standalone';
    handleViewChange: (viewDate: Dayjs, viewMode: DatePickerViewMode) => void;
    private get locale();
    private get viewModeConfig();
    private navigateHandler;
    private handleNavigateToLastYearOrDecade;
    private handleNavigateToNextYearOrDecade;
    private handleNavigateToLastMonth;
    private handleNavigateToNextMonth;
    private get yearClickable();
    private handleYearClick;
    private get monthClickable();
    private handleMonthClick;
    render(): any;
}
