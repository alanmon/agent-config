import { EventEmitter, ComponentInterface } from '../../../stencil-public-runtime';
import { Status } from "../../../shared/base-props/index";
import { TimezoneInfo } from "../../../shared/timezone/index";
import { Dayjs } from 'dayjs';
import { NormalizedDatePickerComparisonProps, DatePickerViewMode, DatePresetOptions, ValueChangeInfo, ComparisonChangeInfo, DateTimeFormatOptions } from '../types';
import { Placement } from '@floating-ui/dom';
/**
 * The DateTimePickerBase component serves two primary purposes:
 * 1. Normalization: It normalizes external prop values into a consistent internal format for use by internal components.
 * 2. Composition: It assembles and orchestrates the various internal components (e.g., Calendar, TimePanel) that constitute the complete date-time picker.
 */
export declare class KsDateTimePickerCore implements ComponentInterface {
    el: HTMLKsDateTimePickerCoreElement;
    clearable: boolean;
    status: Status;
    timezone: TimezoneInfo;
    showTimezone: false | {
        placement: 'input' | 'panel';
        style?: 'longOffset' | 'full';
    };
    placement: Placement;
    open: boolean;
    presets: boolean | {
        options: DatePresetOptions;
    };
    needConfirm: boolean;
    showCancel: boolean;
    viewDate: Dayjs;
    now: Dayjs;
    disabledDate: (date: Dayjs, info: {
        type: 'date' | 'preset';
        from?: Dayjs;
        to?: Dayjs;
        selectionPhase?: 'start' | 'end';
    }) => boolean;
    ksClear: EventEmitter<void>;
    private popupExtraFooterSlot?;
    private popupFooterAppendSlot?;
    private prefixSlot?;
    value: [Dayjs | null, Dayjs | null];
    placeholder: [string | undefined, string | undefined];
    disabled: [boolean, boolean];
    comparison: NormalizedDatePickerComparisonProps;
    setComparison: (comparison: NormalizedDatePickerComparisonProps, reason: ComparisonChangeInfo) => void;
    onComparisonPick: (comparison: NormalizedDatePickerComparisonProps, reason: ComparisonChangeInfo) => void;
    viewMode: DatePickerViewMode;
    availableViewModes: DatePickerViewMode[];
    handleViewChange: (viewDate: Dayjs, viewMode: DatePickerViewMode) => void;
    isRange: boolean;
    /** Whether to render a single DateGrid, or dual to have a broader view of selection. @note Unrelated to `isRange`, purely controls view. */
    displayMode: 'single' | 'dual';
    rangeSelectionMode: 'direct' | '2-step';
    setValue: (value: [Dayjs | null, Dayjs | null], reason: ValueChangeInfo) => void;
    setOpen: (open: boolean) => void;
    displayFormatOptions: DateTimeFormatOptions;
    normalizedFormat: string;
    /** The selectionPhase is only usable for range selection. It will only be `'start'` when not in range selection. */
    private _selectionPhase;
    private isSelectingComparisonValue;
    presetPreviewDate: [Dayjs | null, Dayjs | null] | null;
    private setPresetPreviewDate;
    hoverPart: 'start' | 'end' | null;
    /**
     * This is for storing the draft in `needConfirm` mode.
     * originals do not require to be reactive.
     */
    draft: {
        value: [Dayjs | null, Dayjs | null];
        comparison: NormalizedDatePickerComparisonProps;
    };
    private draftChangeInfo;
    get shouldUseDraft(): boolean;
    get valueWithDraft(): [Dayjs | null, Dayjs | null];
    get comparisonWithDraft(): NormalizedDatePickerComparisonProps;
    private setValueWithDraft;
    private setComparisonWithDraft;
    private setValueWithDraftByPreset;
    private setComparisonWithDraftByPreset;
    private setComparisonWithDraftByToggle;
    valueWatcher(): void;
    comparisonWatcher(newComparison: NormalizedDatePickerComparisonProps, oldComparison: NormalizedDatePickerComparisonProps): void;
    openWatcher(open: boolean): void;
    private handleOpenChange;
    private handleClear;
    private get selectionPhase();
    private updateSelectionPhase;
    private setIsSelectingComparisonValue;
    private setViewDate;
    private handleComparisonValueChangeByPick;
    private handleValueChangeByPick;
    private handleHoverPartChange;
    private handleConfirm;
    private handleCancel;
    private renderTimezoneIndicator;
    private renderFooter;
    render(): any;
}
