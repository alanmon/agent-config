import { TimezoneInfo } from "../../shared/timezone/index";
export declare const aadvid: string;
export declare const isTTAM: boolean;
/** TTAM accounts only has a **fixed** utcOffset, which is **not** a IANA timezone. */
export declare const loadTimezoneInfoOfTTAM: () => Promise<TimezoneInfo | undefined>;
