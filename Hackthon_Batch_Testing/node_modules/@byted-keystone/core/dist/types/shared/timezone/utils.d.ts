import { Timezone, TimezoneInfo } from './types';
export declare function isValidIanaTimezoneId(id?: string): id is string;
export declare function normalizeTimezoneInfo(timezone?: TimezoneInfo, fallback?: TimezoneInfo): TimezoneInfo;
export declare function timezoneToTimezoneInfo(timezone: Timezone): TimezoneInfo;
