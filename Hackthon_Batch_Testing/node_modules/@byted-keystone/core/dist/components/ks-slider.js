import { p as proxyCustomElement, H, d as createEvent, h, F as Fragment, e as Host } from './p-BDWjpCqC.js';
import { s as sendActionTracking } from './p-1kcbQENr.js';
import { r as registerPluginManager } from './p-D1SAjL-M.js';
import { d as dir } from './p-PkW5LtIt.js';
import { f as formatToLocalizedNumber } from './p-ePTxuw5t.js';
import { d as defineCustomElement$2 } from './p-CpTHgzaD.js';
import { n as noop } from './p-D2D-tLJn.js';
import { d as debounce } from './p-6WABMd7m.js';

const valueToPercent = (value, min, max) => ((value - min) * 100) / (max - min);
const percentToValue = (percent, min, max) => (max - min) * percent + min;
const findClosestIndex = (values, value) => values.reduce((pre, cur, index) => {
    const distance = Math.abs(value - cur);
    if (pre === null || distance <= pre.distance) {
        return {
            distance,
            index,
        };
    }
    return pre;
}, null)?.index ?? 0;

const indexCss = ":host{display:inline-block;width:100%}.slider{border-radius:var(--ks-border-radius-md);cursor:pointer;height:4px;padding:6px 0;position:relative;width:100%}.slider__bar,.slider__track{border-radius:inherit;bottom:0;display:block;height:inherit;margin:auto 0;position:absolute;top:0}.slider__bar{background-color:var(--ks-color-neutral-surface3);width:100%}.slider__track{transition-duration:.1s;transition-property:width,left;transition-timing-function:var(--ks-transition-easing-standardOut)}.slider--primary .slider__track{background-color:var(--ks-color-primary-fill)}.slider--primary .slider__indicator,.slider--primary .slider__mark--active{background-color:var(--ks-color-primary-onFill);border-color:var(--ks-color-primary-fill)}.slider--primary:not(.slider--disabled) .slider__indicator:hover{border-color:var(--ks-color-primary-fillHover)}.slider--primary:not(.slider--disabled) .slider__indicator:active{border-color:var(--ks-color-primary-fillActive)}.slider--primary.slider--disabled .slider__track{background-color:var(--ks-color-primary-fillDisabled)}.slider--primary.slider--disabled .slider__indicator,.slider--primary.slider--disabled .slider__mark--active{border-color:var(--ks-color-primary-fillDisabled)}.slider--support .slider__track{background-color:var(--ks-color-support-fill)}.slider--support .slider__indicator,.slider--support .slider__mark--active{background-color:var(--ks-color-support-onFill);border-color:var(--ks-color-support-fill)}.slider--support:not(.slider--disabled) .slider__indicator:hover{border-color:var(--ks-color-support-fillHover)}.slider--support:not(.slider--disabled) .slider__indicator:active{border-color:var(--ks-color-support-fillActive)}.slider--support.slider--disabled .slider__track{background-color:var(--ks-color-support-fillDisabled)}.slider--support.slider--disabled .slider__indicator,.slider--support.slider--disabled .slider__mark--active{border-color:var(--ks-color-support-fillDisabled)}.slider--neutral .slider__track{background-color:var(--ks-color-neutral-fill)}.slider--neutral .slider__indicator,.slider--neutral .slider__mark--active{background-color:var(--ks-color-neutral-onFill);border-color:var(--ks-color-neutral-fill)}.slider--neutral:not(.slider--disabled) .slider__indicator:hover{border-color:var(--ks-color-neutral-fillHover)}.slider--neutral:not(.slider--disabled) .slider__indicator:active{border-color:var(--ks-color-neutral-fillActive)}.slider--neutral.slider--disabled .slider__track{background-color:var(--ks-color-neutral-fillDisabled)}.slider--neutral.slider--disabled .slider__indicator,.slider--neutral.slider--disabled .slider__mark--active{border-color:var(--ks-color-neutral-fillDisabled)}.slider--success .slider__track{background-color:var(--ks-color-success-fill)}.slider--success .slider__indicator,.slider--success .slider__mark--active{background-color:var(--ks-color-success-onFill);border-color:var(--ks-color-success-fill)}.slider--success:not(.slider--disabled) .slider__indicator:hover{border-color:var(--ks-color-success-fillHover)}.slider--success:not(.slider--disabled) .slider__indicator:active{border-color:var(--ks-color-success-fillActive)}.slider--success.slider--disabled .slider__track{background-color:var(--ks-color-success-fillDisabled)}.slider--success.slider--disabled .slider__indicator,.slider--success.slider--disabled .slider__mark--active{border-color:var(--ks-color-success-fillDisabled)}.slider--warning .slider__track{background-color:var(--ks-color-warning-fill)}.slider--warning .slider__indicator,.slider--warning .slider__mark--active{background-color:var(--ks-color-warning-onFill);border-color:var(--ks-color-warning-fill)}.slider--warning:not(.slider--disabled) .slider__indicator:hover{border-color:var(--ks-color-warning-fillHover)}.slider--warning:not(.slider--disabled) .slider__indicator:active{border-color:var(--ks-color-warning-fillActive)}.slider--warning.slider--disabled .slider__track{background-color:var(--ks-color-warning-fillDisabled)}.slider--warning.slider--disabled .slider__indicator,.slider--warning.slider--disabled .slider__mark--active{border-color:var(--ks-color-warning-fillDisabled)}.slider--error .slider__track{background-color:var(--ks-color-error-fill)}.slider--error .slider__indicator,.slider--error .slider__mark--active{background-color:var(--ks-color-error-onFill);border-color:var(--ks-color-error-fill)}.slider--error:not(.slider--disabled) .slider__indicator:hover{border-color:var(--ks-color-error-fillHover)}.slider--error:not(.slider--disabled) .slider__indicator:active{border-color:var(--ks-color-error-fillActive)}.slider--error.slider--disabled .slider__track{background-color:var(--ks-color-error-fillDisabled)}.slider--error.slider--disabled .slider__indicator,.slider--error.slider--disabled .slider__mark--active{border-color:var(--ks-color-error-fillDisabled)}.slider__indicator,.slider__mark{border-radius:var(--ks-border-radius-full);border-style:solid;position:absolute;top:50%;transform:translate(-50%,-50%)}.slider__indicator{border-width:3px;height:16px;transition-duration:.1s;transition-property:left;transition-timing-function:var(--ks-transition-easing-standardOut);width:16px}.slider__indicator .slider__label{color:var(--ks-color-neutral-onSurface);left:50%;top:-26px;transform:translateX(-50%)}.slider__indicator input{display:none}.slider__mark{background-color:var(--ks-color-neutral-onFill);border-color:var(--ks-color-neutral-surface3);border-width:2px;height:8px;width:8px}.slider__label{color:var(--ks-color-neutral-highOnSurface);font-family:var(--ks-text-bodySm-fontFamily);font-size:var(--ks-text-bodySm-fontSize);font-weight:var(--ks-text-bodySm-fontWeight);letter-spacing:var(--ks-text-bodySm-letterSpacing);line-height:var(--ks-text-bodySm-lineHeight);position:absolute;top:18px;transform:translateX(-50%);white-space:nowrap}.slider--disabled{cursor:not-allowed}.slider--labeld{margin-bottom:var(--ks-spacing-5)}.slider__tooltip::part(bodywrap){pointer-events:none}";

const prefix = 'slider';
const KsSlider$1 = /*@__PURE__*/ proxyCustomElement(class KsSlider extends H {
    componentWillLoad() {
        if (this.value === undefined) {
            this.currentValue = this.defaultValue;
        }
        else {
            this.currentValue = this.value;
        }
        this.bindMouseMoveSlider = this.handleMouseMoveSlider.bind(this);
        this.bindMouseUpSlider = this.handleMouseUpSlider.bind(this);
    }
    valueWatcher(val) {
        this.currentValue = val;
    }
    defaultValueWatcher(val) {
        if (this.value === undefined) {
            this.currentValue = val;
        }
    }
    constructor(registerHost) {
        super();
        if (registerHost !== false) {
            this.__registerHost();
        }
        this.__attachShadow();
        this.ksChange = createEvent(this, "ksChange", 1);
        this.ksChangeComplete = createEvent(this, "ksChangeComplete", 1);
        this.dragging = false;
        this.previousIndex = 0;
        this.currentValue = 0;
        this.__showTooltip = false;
        /** The status of the slider. Can be one of the following values: `"default"`, `"error"`. */
        this.status = 'default';
        /** Indicates whether the slider is disabled. When `true`, the slider will be inactive and cannot be interacted with. */
        this.disabled = false;
        /** The minimum value of the slider's range. */
        this.min = 0;
        /** The maximum value of the slider's range. */
        this.max = 100;
        /** The step interval between selectable values on the slider. */
        this.step = 1;
        /** Marks indicating specific values along the slider's range. Can be an array of objects with a `value` and optional `label`, or a boolean to show or hide default marks. */
        this.marks = false;
        // TODO: label api for customizing thumb label
        // /**
        //  * @locale {en} An array of label configurations for the slider's indicators/thumbs. Each object in the array can specify a `label` text and a `show` boolean to control visibility. Corresponds to each value in a range slider or the single value in a standard slider.
        //  * @locale {zh} 滑块指示器/滑块的标签配置数组。数组中的每个对象可以指定 `label` 文本和 `show` 布尔值来控制可见性。对应于范围滑块中的每个值或标准滑块中的单个值。
        //  */
        // @Prop() label: SliderLabel[] = [];
        /** The default value of the slider when it is first rendered. Used if `value` is not provided. */
        this.defaultValue = 0;
        /** Whether to show the tooltip when hover on the indicator/handle. */
        this.tooltipOpen = true;
        /** An array of strings to be displayed as content within the tooltips for each slider indicator/handle. The order of strings corresponds to the order of values in a range slider, or a single string for a standard slider. */
        this.tooltipContent = [];
        this.isHover = false;
        this.bindMouseUpSlider = noop;
        this.bindMouseMoveSlider = noop;
        this.indicatorMouseEnter = debounce(() => {
            if (this.isHover) {
                this.__showTooltip = true;
            }
        }, 200);
        registerPluginManager(this.el);
    }
    getNewValue(x) {
        const { width, left } = this.sliderRef.getBoundingClientRect();
        let percent = (x - left) / width;
        if (dir() === 'rtl') {
            percent = 1 - percent;
        }
        let newValue = percentToValue(percent, this.min, this.max);
        if (this.step) {
            newValue = Math.round((newValue - this.min) / this.step) * this.step + this.min;
        }
        else {
            const values = (Array.isArray(this.marks) ? this.marks : this.steppedMarks).map((x) => x.value);
            // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
            newValue = values[findClosestIndex(values, newValue)];
        }
        newValue = Math.max(this.min, Math.min(newValue, this.max));
        let activeIndex = 0;
        if (Array.isArray(this.currentValue)) {
            activeIndex = this.dragging ? this.previousIndex : findClosestIndex(this.currentValue, newValue);
        }
        return { newValue, activeIndex };
    }
    handleMouseUpSlider(event) {
        this.dragging = false;
        if (this.isHover) {
            this.__showTooltip = true;
        }
        const { newValue, activeIndex } = this.getNewValue(event.clientX);
        if (Array.isArray(this.currentValue)) {
            const newValues = [...this.currentValue];
            newValues.splice(activeIndex, 1, newValue);
            if (this.value === undefined) {
                this.currentValue = newValues;
            }
            this.ksChange.emit(newValues);
            this.ksChangeComplete.emit(newValues);
            sendActionTracking(this.el, { eventType: 'change', componentParams: { value: newValues } });
        }
        else {
            if (this.value === undefined) {
                this.currentValue = newValue;
            }
            this.ksChange.emit(newValue);
            this.ksChangeComplete.emit(newValue);
            sendActionTracking(this.el, { eventType: 'change', componentParams: { value: newValue } });
        }
        document.removeEventListener('mousemove', this.bindMouseMoveSlider);
        document.removeEventListener('mouseup', this.bindMouseUpSlider);
        // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
        document.removeEventListener('touchmove', this.bindMouseMoveSlider);
        // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
        document.removeEventListener('touchend', this.bindMouseUpSlider);
    }
    handleMouseMoveSlider(event) {
        if (event.buttons === 0) {
            return;
        }
        const { newValue, activeIndex } = this.getNewValue(event.clientX);
        if (Array.isArray(this.currentValue)) {
            const newValues = [...this.currentValue];
            newValues.splice(activeIndex, 1, newValue);
            if (this.value === undefined) {
                this.currentValue = newValues;
            }
            this.ksChange.emit(newValues);
        }
        else {
            if (this.value === undefined) {
                this.currentValue = newValue;
            }
            this.ksChange.emit(newValue);
        }
        if (!this.dragging) {
            this.previousIndex = activeIndex;
            this.dragging = true;
        }
    }
    handleMouseDownSlider(event) {
        event.preventDefault();
        if (this.disabled) {
            return;
        }
        this.__showTooltip = false;
        const { newValue, activeIndex } = this.getNewValue(event.clientX);
        if (Array.isArray(this.currentValue)) {
            const newValues = [...this.currentValue];
            newValues.splice(activeIndex, 1, newValue);
            if (this.value === undefined) {
                this.currentValue = newValues;
            }
            this.ksChange.emit(newValues);
        }
        else {
            if (this.value === undefined) {
                this.currentValue = newValue;
            }
            this.ksChange.emit(newValue);
        }
        document.addEventListener('mousemove', this.bindMouseMoveSlider);
        document.addEventListener('mouseup', this.bindMouseUpSlider);
        // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
        document.addEventListener('touchmove', this.bindMouseMoveSlider);
        // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
        document.addEventListener('touchend', this.bindMouseUpSlider);
    }
    get trackOffset() {
        return valueToPercent(Array.isArray(this.currentValue) ? Math.min(...this.currentValue) : this.min, this.min, this.max);
    }
    get trackWidth() {
        return (valueToPercent(Array.isArray(this.currentValue) ? Math.max(...this.currentValue) : this.currentValue, this.min, this.max) - this.trackOffset);
    }
    renderIndicator(value, index) {
        const percent = valueToPercent(value, this.min, this.max);
        const content = this.tooltipContent[index] ||
            (!isNaN(percent) && percent !== Infinity && formatToLocalizedNumber(percent.toString(), 1));
        const showTooltip = this.__showTooltip && content ? true : false;
        return (h("ks-tooltip-1-1-1w", { class: `${prefix}__tooltip`, disabled: !this.tooltipOpen, open: showTooltip, content: content || '' }, h("span", { onMouseEnter: () => {
                this.isHover = true;
                this.indicatorMouseEnter();
            }, onMouseOut: () => {
                this.__showTooltip = false;
                this.isHover = false;
            }, "data-index": index, class: `${prefix}__indicator`, part: "handle", style: { [dir() === 'rtl' ? 'right' : 'left']: `${percent}%` }, "data-testid": "ks-slider-index-44tQGY" }, h("input", { type: "range", min: this.min, max: this.max, step: this.step, value: value, "aria-valuemax": this.max, "aria-valuemin": this.min, "aria-valuenow": value, "aria-valuetext": value }))));
    }
    get steppedMarks() {
        return [...Array(Math.floor((this.max - this.min) / this.step) + 1)].map((_, index) => ({
            value: this.min + this.step * index,
        }));
    }
    renderMarks(mark, index) {
        const percent = valueToPercent(mark.value, this.min, this.max);
        const isActive = Array.isArray(this.currentValue)
            ? mark.value >= Math.min(...this.currentValue) && mark.value <= Math.max(...this.currentValue)
            : mark.value <= this.currentValue;
        return (h(Fragment, null, h("span", { "data-index": index, class: { [`${prefix}__mark`]: true, [`${prefix}__mark--active`]: isActive }, style: { [dir() === 'rtl' ? 'right' : 'left']: `${percent}%` } }), mark.label && (h("span", { "data-index": index, class: `${prefix}__label`, style: { [dir() === 'rtl' ? 'right' : 'left']: `${percent}%` }, "aria-hidden": "true" }, mark.label))));
    }
    render() {
        const variant = this.status === 'default' ? 'primary' : 'error';
        const classes = {
            [prefix]: true,
            [`${prefix}--${variant}`]: true,
            [`${prefix}--disabled`]: this.disabled,
            [`${prefix}--labeled`]: Array.isArray(this.marks) && this.marks.some((x) => x.label),
        };
        return (h(Host, { key: 'f8f434874d88cae00cd3a8e7635b3a9685af89c7', class: "KsSlider", role: "slider" }, h("div", { key: '165e8023cc06249fcd030195e29b5ad64c175025', class: classes, ref: (el) => (this.sliderRef = el), onMouseDown: this.handleMouseDownSlider.bind(this),
            // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
            onTouchStart: this.handleMouseDownSlider.bind(this), "data-testid": "ks-slider-index-i2ckxA" }, h("span", { key: 'c67aaeae10f0a9865fc153c282a543d727ddb89e', class: `${prefix}__bar` }), h("span", { key: 'f102192597a9414219cb82f608118ba7e5e8288d', class: `${prefix}__track`, style: { [dir() === 'rtl' ? 'right' : 'left']: `${this.trackOffset}%`, width: `${this.trackWidth}%` } }), typeof this.marks === 'boolean'
            ? this.marks && this.steppedMarks.map(this.renderMarks.bind(this))
            : this.marks.map(this.renderMarks.bind(this)), typeof this.currentValue === 'number'
            ? this.renderIndicator(this.currentValue, 0)
            : this.currentValue.map(this.renderIndicator.bind(this)))));
    }
    get el() { return this; }
    static get watchers() { return {
        "value": ["valueWatcher"],
        "defaultValue": ["defaultValueWatcher"]
    }; }
    static get style() { return indexCss; }
}, [257, "ks-slider", {
        "status": [1],
        "disabled": [4],
        "min": [2],
        "max": [2],
        "step": [2],
        "marks": [4],
        "value": [2],
        "defaultValue": [2, "default-value"],
        "tooltipOpen": [4, "tooltip-open"],
        "tooltipContent": [16],
        "currentValue": [32],
        "__showTooltip": [32]
    }, undefined, {
        "value": ["valueWatcher"],
        "defaultValue": ["defaultValueWatcher"]
    }]);
function defineCustomElement$1() {
    if (typeof customElements === "undefined") {
        return;
    }
    const components = ["ks-slider-1-1-1w", "ks-tooltip-1-1-1w"];
    components.forEach(tagName => { switch (tagName) {
        case "ks-slider-1-1-1w":
            if (!customElements.get(tagName)) {
                customElements.define(tagName, KsSlider$1);
            }
            break;
        case "ks-tooltip-1-1-1w":
            if (!customElements.get(tagName)) {
                defineCustomElement$2();
            }
            break;
    } });
}

const KsSlider = KsSlider$1;
const defineCustomElement = defineCustomElement$1;

export { KsSlider, defineCustomElement };
//# sourceMappingURL=ks-slider.js.map

//# sourceMappingURL=ks-slider.js.map