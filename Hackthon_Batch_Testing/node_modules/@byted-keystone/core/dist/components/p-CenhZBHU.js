import { H } from './p-BDWjpCqC.js';
import { F as FormContext } from './p-B7IgmzGx.js';
import { C as Consume } from './p-D0NPoP6r.js';
import { P as Provide } from './p-DybOb_qH.js';

var __decorate = (undefined && undefined.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function")
        r = Reflect.decorate(decorators, target, key, desc);
    else
        for (var i = decorators.length - 1; i >= 0; i--)
            if (d = decorators[i])
                r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
const FormBaseComponent = class extends H {
    constructor() {
        super(false);
    }
    onKsChangeForForm({ detail }) {
        if (this._provider === 'formItem') {
            if (detail?._isDetailArgs) {
                const [value] = (detail || []);
                this.callbacksFromContext?.onValueChange?.(value, []);
            }
            else {
                this.callbacksFromContext?.onValueChange?.(detail, []);
            }
        }
    }
    onKsBlurForForm() {
        this.callbacksFromContext?.onBlur?.();
    }
};
__decorate([
    Provide({
        context: FormContext,
    })
], FormBaseComponent.prototype, "__contextBlocker", void 0);
__decorate([
    Consume({
        context: FormContext,
        subscribe: true,
        path: ['_provider'],
    })
], FormBaseComponent.prototype, "_provider", void 0);
__decorate([
    Consume({
        context: FormContext,
        path: ['callbacks'],
    })
], FormBaseComponent.prototype, "callbacksFromContext", void 0);
__decorate([
    Consume({
        context: FormContext,
        subscribe: true,
        path: ['value'],
    })
], FormBaseComponent.prototype, "valueFromContext", void 0);
__decorate([
    Consume({
        context: FormContext,
        subscribe: true,
        path: ['status'],
    })
], FormBaseComponent.prototype, "statusFromContext", void 0);
__decorate([
    Consume({
        context: FormContext,
        subscribe: true,
        path: ['disabled'],
    })
], FormBaseComponent.prototype, "disabledFromContext", void 0);

function FormContextValueReconcile({ providerKey = '_provider', contextKey, } = {}) {
    return (target, propertyKey) => {
        contextKey = contextKey || `${propertyKey}FromContext`;
        const originalValueDescriptor = Object.getOwnPropertyDescriptor(target, propertyKey);
        const originalContextValueDescriptor = Object.getOwnPropertyDescriptor(target, contextKey);
        // Should use a map to distinguish different components
        const isUpdatingInternalStateMap = new WeakMap();
        if (!originalValueDescriptor) {
            return;
        }
        // original value from user
        const valueMap = new WeakMap();
        Object.defineProperty(target, propertyKey, {
            configurable: true,
            enumerable: true,
            set(value) {
                if (isUpdatingInternalStateMap.get(this)) {
                    originalValueDescriptor.set?.call(this, value);
                    return;
                }
                // 劫持 value 变化，记录原始值
                valueMap.set(this, value);
                originalValueDescriptor.set?.call(this, value);
            },
        });
        Object.defineProperty(target, contextKey, {
            configurable: true,
            enumerable: true,
            set(value) {
                isUpdatingInternalStateMap.set(this, true);
                // 劫持 contextValue 变化，并在 value 原始值是 undefined 时改变 value
                originalContextValueDescriptor?.set?.call(this, value);
                if (this[providerKey] === 'formItem') {
                    const uncontrollable = valueMap.get(this) === undefined;
                    if (uncontrollable) {
                        this[propertyKey] = value;
                    }
                }
                isUpdatingInternalStateMap.set(this, false);
            },
        });
    };
}

export { FormBaseComponent as F, FormContextValueReconcile as a };
//# sourceMappingURL=p-CenhZBHU.js.map

//# sourceMappingURL=p-CenhZBHU.js.map