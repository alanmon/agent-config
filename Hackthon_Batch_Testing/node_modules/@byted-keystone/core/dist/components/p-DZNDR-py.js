function formatCSSLength(length) {
    if (typeof length === 'string') {
        if (!isNaN(Number(length))) {
            return `${length}px`;
        }
        return length;
    }
    else if (typeof length === 'number') {
        return `${length}px`;
    }
    return;
}
function parseCSSLength(length = 0) {
    if (typeof length === 'number') {
        return length;
    }
    else {
        if (length?.endsWith('px')) {
            return Number(length.replace('px', ''));
        }
        return Number(length);
    }
}

export { formatCSSLength as f, parseCSSLength as p };
//# sourceMappingURL=p-DZNDR-py.js.map

//# sourceMappingURL=p-DZNDR-py.js.map