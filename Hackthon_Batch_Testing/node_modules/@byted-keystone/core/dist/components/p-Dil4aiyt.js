import { p as proxyCustomElement, H, d as createEvent, h, e as Host } from './p-BDWjpCqC.js';
import { c as classnames } from './p-BLeWSNPN.js';
import { a as attrAccept, d as defaultRequest } from './p-DjGqRc_g.js';
import { S as Slot } from './p-DGhTWl9g.js';
import { g as generateUniqueId } from './p-Dl8qMZy-.js';
import { i as isObjectLike, b as baseGetTag } from './p-MkO7EzJx.js';

/** `Object#toString` result references. */
var numberTag = '[object Number]';

/**
 * Checks if `value` is classified as a `Number` primitive or object.
 *
 * **Note:** To exclude `Infinity`, `-Infinity`, and `NaN`, which are
 * classified as numbers, use the `_.isFinite` method.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a number, else `false`.
 * @example
 *
 * _.isNumber(3);
 * // => true
 *
 * _.isNumber(Number.MIN_VALUE);
 * // => true
 *
 * _.isNumber(Infinity);
 * // => true
 *
 * _.isNumber('3');
 * // => false
 */
function isNumber(value) {
  return typeof value == 'number' ||
    (isObjectLike(value) && baseGetTag(value) == numberTag);
}

var __decorate = (undefined && undefined.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function")
        r = Reflect.decorate(decorators, target, key, desc);
    else
        for (var i = decorators.length - 1; i >= 0; i--)
            if (d = decorators[i])
                r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
const prefix = 'rc-upload';
const KsRcUpload = /*@__PURE__*/ proxyCustomElement(class KsRcUpload extends H {
    constructor(registerHost) {
        super();
        if (registerHost !== false) {
            this.__registerHost();
        }
        this.__attachShadow();
        this.ksChange = createEvent(this, "ksChange", 1);
        this.reqs = {};
        this.name = 'file';
        this.disabled = false;
        this.multiple = false;
        this.method = 'post';
        this.fileListLength = 0;
        this.processFile = async (file, fileList) => {
            let transformedFile = file;
            if (this.beforeUpload) {
                let beforeUploadHookReturn;
                try {
                    beforeUploadHookReturn = await this.beforeUpload(file, fileList);
                }
                catch (e) {
                    // Rejection will also trade as false
                    beforeUploadHookReturn = false;
                }
                if (beforeUploadHookReturn === false) {
                    return {
                        origin: file,
                        parsedFile: null,
                        action: null,
                        data: null,
                    };
                }
                else {
                    transformedFile = beforeUploadHookReturn;
                }
            }
            // Get latest action
            const { action } = this;
            let mergedAction;
            if (typeof action === 'function') ;
            else {
                mergedAction = action;
            }
            // Get latest data
            const { data } = this;
            let mergedData;
            if (typeof data === 'function') {
                mergedData = await data(transformedFile);
            }
            else {
                // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
                mergedData = data;
            }
            let parsedFile;
            if (transformedFile instanceof File) {
                parsedFile = transformedFile;
            }
            else {
                // Not sure if this will work since no related test case works with it
                parsedFile = new File([transformedFile], file.name, { type: file.type });
            }
            const mergedParsedFile = parsedFile;
            // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
            mergedParsedFile.uid = file.uid;
            return {
                origin: file,
                data: mergedData,
                parsedFile: mergedParsedFile,
                // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
                action: mergedAction,
            };
        };
        this.onTrigger = () => {
            if (this.disabled) {
                return;
            }
            this.fileInput?.click();
        };
        // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
        this.onFileDrop = (e) => {
            e.preventDefault();
            let files = [...e.dataTransfer.files].filter((file) => attrAccept(file, this.accept));
            if (!this.multiple) {
                files = files.slice(0, 1);
            }
            if (files.length === 0) {
                this.onfail?.(new CustomEvent('onfail', {
                    bubbles: false,
                    composed: false,
                    detail: {
                        error: 'file accept err',
                        response: 'error',
                        file: files,
                    },
                }));
            }
            this.uploadFiles(files);
        };
        // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
        this.uploadChange = (e) => {
            this.uploadFiles(e.target.files);
        };
    }
    handleUpdate() {
        this.children.forEach((item) => {
            // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
            item.disabled = this.disabled;
        });
    }
    uploadFiles(files) {
        let value;
        let behavior;
        if (isNumber(this.maxCount)) {
            value = this.maxCount;
        }
        else if (this.maxCount && typeof this.maxCount === 'object') {
            if (isNumber(this.maxCount.value)) {
                value = this.maxCount.value;
            }
            behavior = this.maxCount.behavior;
        }
        if (!behavior) {
            behavior = 'reject';
        }
        let originFiles = [...files];
        if (isNumber(value) && behavior === 'reject') {
            const capacity = value - this.fileListLength;
            if (capacity <= 0) {
                this.clearInputValue();
                return;
            }
            originFiles = originFiles.slice(0, capacity);
        }
        if (originFiles.length === 0) {
            this.clearInputValue();
            return;
        }
        const postFiles = originFiles.map((file) => {
            // eslint-disable-next-line no-param-reassign
            file.uid = generateUniqueId();
            return this.processFile(file, originFiles);
        });
        Promise.all(postFiles)
            .then((fileList) => {
            fileList
                // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
                .filter((file) => file.parsedFile !== null)
                .forEach((file) => {
                // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
                this.post(file);
            });
        })
            .finally(() => {
            this.clearInputValue();
        });
    }
    // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
    post({ data, origin, action, parsedFile }) {
        const { name, headers, method } = this;
        const { uid } = origin;
        const request = this.customRequest || defaultRequest;
        const requestOption = {
            action,
            filename: name,
            data,
            file: parsedFile,
            headers,
            method,
            onProgress: (event) => {
                // this.onpro?.(e, parsedFile);
                this.onpro?.(new CustomEvent('onpro', {
                    bubbles: false,
                    composed: false,
                    detail: {
                        event,
                        file: parsedFile,
                    },
                }));
            },
            // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
            onSuccess: (ret, xhr) => {
                this.onsuccess?.(new CustomEvent('onsuccess', {
                    bubbles: false,
                    composed: false,
                    detail: {
                        response: ret,
                        file: parsedFile,
                        xhr,
                    },
                }));
                // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
                delete this.reqs[uid];
            },
            // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
            onError: (err, ret) => {
                // const { onfail1 } = this;
                this.onfail?.(new CustomEvent('onfail', {
                    bubbles: false,
                    composed: false,
                    detail: {
                        error: err,
                        response: ret,
                        file: parsedFile,
                    },
                }));
            },
        };
        // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
        this.reqs[uid] = request(requestOption);
    }
    /** Internal */
    // eslint-disable-next-line @typescript-eslint/require-await
    // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
    async abort(file) {
        const { reqs } = this;
        if (file) {
            const uid = file.uid ? file.uid : file;
            // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
            if (reqs[uid]?.abort) {
                // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
                reqs[uid].abort();
            }
            // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
            delete reqs[uid];
        }
        else {
            Object.keys(reqs).forEach((uid) => {
                // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
                if (reqs[uid]?.abort) {
                    // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
                    reqs[uid].abort();
                }
                // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
                delete reqs[uid];
            });
        }
    }
    componentWillLoad() {
        this.children.forEach((item) => {
            // @ts-expect-error TYPE_FIXME. Please Fix the below strict mode type error. Read https://bytedance.sg.larkoffice.com/docx/MIsKdGOQwoQsnuxrsxEl6AsYgCf
            item.disabled = this.disabled;
        });
    }
    /** Internal */
    async clearInputValue() {
        if (!this.fileInput) {
            return;
        }
        this.fileInput.value = '';
    }
    render() {
        return (h(Host, { key: '12c26f1b4deadec306a894fbe583e71bac3d7add', class: "KsRcUpload" }, h("span", { key: '83b938272d7b0b68701ee988911f5486fd110c1d', class: classnames(prefix), part: "self", onClick: this.onTrigger, onDrop: this.onFileDrop, "data-testid": "rc-upload-rc-upload-8JzfyY" }, h("input", { key: '0f19954c9ec1a2323458e30a9ec0b724052639b3', part: "upload", type: "file", accept: this.accept, ref: (el) => (this.fileInput = el), onClick: (e) => e.stopPropagation(), style: { display: 'none' }, multiple: this.multiple, disabled: this.disabled, onChange: this.uploadChange, "data-testid": "rc-upload-rc-upload-rrbezU" }), h("slot", { key: '17654ad9b89bc47b4a879fc82b49d21dbb29b16c' }))));
    }
    get el() { return this; }
    static get watchers() { return {
        "disabled": ["handleUpdate"]
    }; }
}, [257, "ks-rc-upload", {
        "accept": [1],
        "action": [1],
        "data": [16],
        "name": [1],
        "disabled": [4],
        "multiple": [4],
        "headers": [16],
        "method": [1],
        "customRequest": [16],
        "beforeUpload": [16],
        "onstart": [16],
        "onfail": [16],
        "onpro": [16],
        "onsuccess": [16],
        "maxCount": [2, "max-count"],
        "fileListLength": [2, "file-list-length"],
        "abort": [64],
        "clearInputValue": [64]
    }, undefined, {
        "disabled": ["handleUpdate"]
    }]);
__decorate([
    Slot({ slotname: '_default' })
], KsRcUpload.prototype, "children", void 0);
function defineCustomElement() {
    if (typeof customElements === "undefined") {
        return;
    }
    const components = ["ks-rc-upload-1-1-1w"];
    components.forEach(tagName => { switch (tagName) {
        case "ks-rc-upload-1-1-1w":
            if (!customElements.get(tagName)) {
                customElements.define(tagName, KsRcUpload);
            }
            break;
    } });
}

export { KsRcUpload as K, defineCustomElement as d, isNumber as i };
//# sourceMappingURL=p-Dil4aiyt.js.map

//# sourceMappingURL=p-Dil4aiyt.js.map