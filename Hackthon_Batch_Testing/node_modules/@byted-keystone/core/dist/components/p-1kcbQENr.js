const COMMON_TRACKING_EVENT_NAME = 'ks:tracking:common';
const DURATION_TRACKING_EVENT_NAME = 'ks:tracking:duration';

const getTrackingEvent = (eventName, type) => (el, params) => {
    el.dispatchEvent(new CustomEvent(eventName, {
        detail: { type, ...params },
    }));
};
/**
 * Send action type tracking event.
 *
 * @param el - HTMLElement instance
 * @param params - See {@link CommonEventParams}
 */
const sendActionTracking = getTrackingEvent(COMMON_TRACKING_EVENT_NAME, 'action');
/**
 * Send expose type tracking event.
 *
 * @param el - HTMLElement instance
 * @param params - See {@link CommonEventParams}
 */
const sendExposeTracking = getTrackingEvent(COMMON_TRACKING_EVENT_NAME, 'expose');
/**
 * Send duration type tracking event.
 *
 * @param el - HTMLElement instance
 * @param params - See {@link DurationEventParams}
 */
const sendDurationTracking = getTrackingEvent(DURATION_TRACKING_EVENT_NAME, 'duration');

export { sendExposeTracking as a, sendDurationTracking as b, sendActionTracking as s };
//# sourceMappingURL=p-1kcbQENr.js.map

//# sourceMappingURL=p-1kcbQENr.js.map