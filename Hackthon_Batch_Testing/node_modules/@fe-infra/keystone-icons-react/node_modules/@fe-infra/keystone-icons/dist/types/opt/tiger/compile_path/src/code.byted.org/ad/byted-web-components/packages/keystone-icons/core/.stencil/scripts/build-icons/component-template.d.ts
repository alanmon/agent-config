import type { IComponentTemplateProps } from './types';
declare const _default: ({ tagName, className, hostClassName, props, events, element, styleUrls, }: IComponentTemplateProps) => string;
export default _default;
