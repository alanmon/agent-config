"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseIconAttributes = exports.getIconCompatClassName = void 0;
const getPascalIconName = (name) => name
    ?.split('-')
    .filter(Boolean)
    .map((segment) => segment.charAt(0).toUpperCase() + segment.slice(1))
    .join('');
const getIconCompatClassName = (rtl, name) => {
    const iconName = getPascalIconName(name);
    return ['ks-icon', rtl ? 'ks-icon--rtlable' : '', 'KsIcon', iconName ? `KsIcon${iconName}` : '']
        .filter(Boolean)
        .join(' ');
};
exports.getIconCompatClassName = getIconCompatClassName;
const parseIconAttributes = (element, manifest) => {
    const size = Number.parseInt(element.getAttribute('size') || '', 10);
    return {
        size: Number.isNaN(size) ? undefined : size,
        color: element.getAttribute('color') || undefined,
        ksV: element.getAttribute('ks-v') || undefined,
        innerColor: manifest.innerColor === undefined ? undefined : element.getAttribute('inner-color') || undefined,
    };
};
exports.parseIconAttributes = parseIconAttributes;
//# sourceMappingURL=attributes.js.map