export const BASE_STYLE = `
:host {
  display: flex;
  align-items: center;
  justify-content: center;
}`;
export const RTL_STYLE = `
:host(:dir(rtl)) {
  transform: scaleX(-1);
}`;
export const SPIN_STYLE = `
:host {
  animation: spin 1s linear infinite;
}
@keyframes spin {
  from { transform: rotate(0deg) }
  to { transform: rotate(360deg) }
}`;
export const buildStyles = ({ rtl, spin }) => [BASE_STYLE, rtl ? RTL_STYLE : '', spin ? SPIN_STYLE : ''].filter(Boolean).join('\n');
//# sourceMappingURL=styles.js.map